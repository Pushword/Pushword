{#
    image
    date
    hLevel
    title
#}
{% if page is defined %}
 {% set image = page.mainImage %}
 {% set date = page.createdAt %}
 {% set title = page.content.h1 %}
{% endif %}


{% block card %}
    {% import view('/component/macro.html.twig') as helper %}
<article class="overflow-hidden rounded-lg shadow-lg">

    {% if image is defined and image is not null %}
        {{ helper.thumb(img_to_media(image), page ?? null, 'thumb', {'class': 'block w-full h-auto m-0'}) }}
    {% endif %}

        <span class="block p-2 text-sm text-white bg-primary">
            {{ date|date('d/m/Y à H:i') }}
        </span>
        <h{{ hLevel|default(2) }} class="p-2 mt-0 text-md" style="margin-bottom:0.5rem">
            <a href="{{ page(page) }}" style="text-decoration:none">
                {{ title|raw }}
            </a>
        </h{{ hLevel|default(2) }}>
    </header>

</article>
{% endblock %}