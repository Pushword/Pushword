{# Backward-compat shim — all logic now lives in the image() Twig function #}
{{ image(image ?? image_src ?? imageSrc,
    alt: image_alt ?? imageAlt ?? null,
    class: image_class ?? null,
    lazy: lazy ?? true,
    attr: image_attr ?? [],
    link: image_link ?? null,
    linkAttr: image_link_attr ?? [],
    obfuscate: image_link_obf ?? true,
    wrapper: image_wrapper ?? null,
    wrapperClass: image_wrapper_class ?? null,
    pictureAttr: picture_attr ?? []) }}
