{% block list %}{% apply spaceless %}
    {% if pages is not empty %}
        <ul>
            {% for page in pages %}
                <li>
                    <a href="{{ page(page) }}">
                        {{ pw(page).h1|raw }}
                    </a>
                </li>
            {% endfor %}
        </ul>
    {% else %}
        <p>...</p>
    {% endif %}
{% endapply %}{% endblock %}
