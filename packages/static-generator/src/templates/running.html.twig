{% extends '@EasyAdmin/page/content.html.twig' %}

{% block content_title %}{{ 'adminStaticGenerating'|trans }}{% endblock %}

{% block page_actions %}
    <a class="btn btn-outline-secondary" href="{{ path('admin_static_generator') }}">
        <i class="fa fa-sync-alt me-2" aria-hidden="true"></i>{{ 'adminLabelUpdate'|trans }}
    </a>
{% endblock %}

{% block main %}
    <meta http-equiv="refresh" content="3">
    <div class="alert alert-info" role="alert">
        <div class="d-flex align-items-center mb-2">
            <i class="fa fa-circle-notch fa-spin me-2"></i>
            <strong>{{ 'adminStaticPleaseWait'|trans }}</strong>
        </div>
        {% if startTime %}
            <p class="mb-1"><small>{{ 'adminStaticStartedAt'|trans }}: {{ startTime|date('d/m/Y à H:i:s') }}</small></p>
        {% endif %}
        {% if host %}
            <p class="mb-0"><strong>{{ 'adminStaticHost'|trans }}:</strong> {{ host }}</p>
        {% endif %}
    </div>
{% endblock %}
