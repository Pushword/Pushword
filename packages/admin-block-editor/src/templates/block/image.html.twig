{% if data.stretched %}<div class="w-full-vw">{% endif %}
{% if data.withBackground %}<div class="px-12 py-1 text-center bg-gray-200 rounded">{% endif %}

    {% include view('/component/image_inline.html.twig') with {
        'image_src': data.file.media|image('default'),
        'image_alt': data.caption,
        'image_class' : ('w-full h-auto ' ~ (data.withBorder ? 'border border-gray-500' : ''))|trim
    } only %}

{% if data.withBackground %}</div>{% endif %}
{% if data.stretched %}</div>{% endif %}
