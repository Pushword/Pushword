{% set images = [] %}
{% for image in block.data %}
    {% set alt = image.caption ? image.caption : image.file.name ~ (images.key is defined ? (' - ' ~ image.file.media) : '') %}
    {% set images = images|merge({(alt): image.url}) %}
{% endfor %}


{{ gallery(images, pos=pos, clickable=block.data.clickable ?? true)|unprose}}