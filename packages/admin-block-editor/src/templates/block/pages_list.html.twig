
{% if (needBlockWrapper(block)) %}<div{{ blockWrapperAttr(block) }}>{% endif %}

{% if (block.data.display == 'card') %}
{{ pages_list(block.data.kw, [(block.data.max ? block.data.max : '9'), (block.data.maxPages ? block.data.maxPages : '1')], block.data.order, 'card', '', page)|unprose }}
{% else %}
{{ pages_list(block.data.kw, [(block.data.max ? block.data.max : '9'), (block.data.maxPages ? block.data.maxPages : '1')], block.data.order, 'list', '', page) }}
{% endif %}

{% if (needBlockWrapper(block)) %}</div>{% endif %}
