{{ pages_list(
    block.data.kw,
    [(block.data.max ? block.data.max : '9'),
    (block.data.maxPages ? block.data.maxPages : '1')],
    block.data.order, block.data.display == 'card' ? 'card' : 'list',
    '',
    page, block.tunes.anchor ?? ''
) }}