<table{{ blockWrapperAttr(block, {class: 'min-w-full divide-y divide-gray-200'}) }}>
    {% for row in block.data.content %}
        <tr>
            {% for cell in row %}
                <td>
                    {{ cell|raw }}
                </td>
            {% endfor %}
        </tr>
    {% endfor %}
</table>
