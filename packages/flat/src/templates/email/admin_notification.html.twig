<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <style>
        body { font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif; line-height: 1.6; color: #1f2937; margin: 0; padding: 0; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .header { background: {% if notification.type == 'conflict' %}#dc2626{% elseif notification.type == 'sync_error' %}#d97706{% else %}#2563eb{% endif %}; color: white; padding: 20px; border-radius: 8px 8px 0 0; }
        .content { background: #f9fafb; padding: 20px; border: 1px solid #e5e7eb; border-top: none; }
        .footer { background: #f3f4f6; padding: 15px 20px; border: 1px solid #e5e7eb; border-top: none; border-radius: 0 0 8px 8px; font-size: 12px; color: #6b7280; }
        .message { background: white; padding: 15px; border-radius: 6px; border-left: 4px solid {% if notification.type == 'conflict' %}#dc2626{% elseif notification.type == 'sync_error' %}#d97706{% else %}#2563eb{% endif %}; }
        .metadata { margin-top: 15px; }
        .metadata dt { font-weight: 600; color: #374151; }
        .metadata dd { margin-left: 0; margin-bottom: 8px; color: #6b7280; }
        h1 { margin: 0; font-size: 20px; }
        .badge { display: inline-block; padding: 4px 8px; border-radius: 4px; font-size: 12px; font-weight: 600; background: rgba(255,255,255,0.2); }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>
                {% if notification.type == 'conflict' %}
                    Conflict Detected
                {% elseif notification.type == 'sync_error' %}
                    Sync Error
                {% else %}
                    Lock Information
                {% endif %}
            </h1>
        </div>

        <div class="content">
            <div class="message">
                {{ notification.message }}
            </div>

            {% if notification.metadata is not empty %}
            <dl class="metadata">
                {% if notification.metadata.entityType is defined %}
                <dt>Entity Type</dt>
                <dd>{{ notification.metadata.entityType }}</dd>
                {% endif %}

                {% if notification.metadata.entityId is defined %}
                <dt>Entity ID</dt>
                <dd>{{ notification.metadata.entityId }}</dd>
                {% endif %}

                {% if notification.metadata.winner is defined %}
                <dt>Winner</dt>
                <dd>{{ notification.metadata.winner }}</dd>
                {% endif %}

                {% if notification.metadata.backupFile is defined %}
                <dt>Backup File</dt>
                <dd>{{ notification.metadata.backupFile }}</dd>
                {% endif %}
            </dl>
            {% endif %}
        </div>

        <div class="footer">
            <strong>Host:</strong> {{ notification.host|default('All hosts') }} |
            <strong>Date:</strong> {{ notification.createdAt|date('Y-m-d H:i:s') }} |
            <strong>Type:</strong> {{ notification.type }}
        </div>
    </div>
</body>
</html>
