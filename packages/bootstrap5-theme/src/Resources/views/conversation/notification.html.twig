
{% for message in messages %}
<div style="margin-bottom:30px;">
    <div style="padding:10px; background:#eee; border-radius:5px; margin-bottom:10px;">{{ message.content|markdown }}</div>

    <p>Envoyé par &lt;{{ message.authorEmail }} {{ message.authorName }}&gt;<br>
    <a href="#">Editer</a></p>
</div>
{% endfor %}