{% extends '@SonataAdmin/CRUD/list_outer_rows_mosaic.html.twig' %}

{% block sonata_mosaic_default_view %}
   {% if object.ratioLabel is defined and object.ratioLabel is not null %}
    <span class="mosaic-box-label label label-default" style="position: absolute; left:10px; right:unset">
      {{ object.ratioLabel }}
    </span>
    {% endif %}

    {{ parent() }}
{% endblock %}