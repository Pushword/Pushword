{
  "devDependencies": {
    "@pushword/js-helper": "^0.0"
  },
  "license": "UNLICENSED",
  "private": true,
  "scripts": {
    "dev-server": "encore dev-server",
    "dev": "encore dev",
    "watch": "TAILWIND_MODE=watch NODE_ENV=development encore dev --watch",
    "build": "NODE_ENV=production yarn encore production"
  }
}
